<%= form_for(@ticket_price) do |f| %>
  <% if @ticket_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_price.errors.count, "error") %> prohibited this ticket_price from being saved:</h2>

      <ul>
      <% @ticket_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no_of_zones %><br />
    <%= f.number_field :no_of_zones %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
